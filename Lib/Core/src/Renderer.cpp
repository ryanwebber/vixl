#include <VX/Core/Assert.h>
#include <VX/Core/Renderer.h>

namespace VX::Core {
    void Renderer::add_hook()
    {
        VX_CORE_ASSERT_NOT_REACHED();
    }

    void Renderer::render_frame() const
    {
        VX_CORE_ASSERT_NOT_REACHED();
    }
}
